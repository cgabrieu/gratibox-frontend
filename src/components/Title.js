import styled from "styled-components";

export default styled.h1`
  font-size: 26px;
  color: #ffffff;
  font-weight: bold;
  text-align: center;
  margin-bottom: ${({ marginBottom }) => marginBottom};
`;
